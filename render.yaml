services:
  - type: web
    name: tikjogos2
    env: node
    buildCommand: npm install --legacy-peer-deps && npm run build
    startCommand: npx tsx server/migrate.ts && npm run start
    envVars:
      - key: NODE_VERSION
        value: 20.11.0
